!function() {
    const e = document;
const t = e.documentElement;
const n = e.body;
const i = e.getElementById("lights-toggle");
const s = window.sr = ScrollReveal();
    function onLoadEvent(){
        a("onLoad");
    }
    function onChangeEvent(){
        a("onChange");
    }
    function a(source) {
        let e = i.parentNode.querySelector(".label-text");
        if(sessionStorage.getItem("windowMode")!=null && sessionStorage.getItem("windowMode") == "darkMode" && source == "onLoad"){
            i.checked = false;
        }
        i.checked ? (n.classList.remove("lights-off"), e && (e.innerHTML = "dark") , (sessionStorage.setItem("windowMode" , "lightMode"))) : (n.classList.add("lights-off"), 
        e && (e.innerHTML = "light"), (sessionStorage.setItem("windowMode" , "darkMode")));
        
    }
    t.classList.remove("no-js"), t.classList.add("js"), window.addEventListener("load", (function() {
        n.classList.add("is-loaded");
    })), n.classList.contains("has-animations") && window.addEventListener("load", (function() {
        s.reveal(".feature", {
            duration: 600,
            distance: "20px",
            easing: "cubic-bezier(0.215, 0.61, 0.355, 1)",
            origin: "right",
            viewFactor: .2
        });
    })), i && (window.addEventListener("load", onLoadEvent), i.addEventListener("change", onChangeEvent));
}();